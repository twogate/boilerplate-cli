startSigil: "{{"
endSigil: "}}"
templateDir: .
outDir: /tmp

replaces:
    - template: "target.txt"
      out: "target.txt"
      placeholders:
         Principessa: "principe"
         stelle: "luna"
